<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Your Wallet </title>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
        <link rel="icon" href="/userTitleLogo/Gemini_Generated_Image_kb9bqfkb9bqfkb9b (1).png" type="image/png">

    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
        }

        body {
            background-color: #f8fafc;
            color: #1a202c;

        }

        .container {
            max-width: 1200px;
            margin: 0 auto;
            padding: 20px;
        }

        header {
            display: flex;
            justify-content: space-between;
            align-items: center;
            padding:  0px;
            margin-bottom: 30px;
            border-bottom: 1px solid #e2e8f0;
        }

        .logo {
            font-size: 24px;
            font-weight: 700;
            color: #2d3748;
        }

        .logo span {
            color: #38a169;
        }

        .user-info {
            display: flex;
            align-items: center;
            gap: 15px;
        }

        .user-avatar {
            width: 40px;
            height: 40px;
            border-radius: 50%;
            background-color: #38a169;
            color: white;
            display: flex;
            align-items: center;
            justify-content: center;
            font-weight: 600;
        }

        .wallet-header {
            text-align: center;
            margin-bottom: 40px;
        }

        .wallet-header h1 {
            font-size: 32px;
            color: #2d3748;
            margin-bottom: 10px;
        }

        .wallet-header p {
            color: #718096;
            font-size: 18px;
            max-width: 600px;
            margin: 0 auto;
        }

        .wallet-grid {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
            gap: 25px;
            margin-bottom: 40px;
        }

        .card {
            background: white;
            border-radius: 16px;
            box-shadow: 0 4px 20px rgba(0, 0, 0, 0.08);
            padding: 25px;
            transition: transform 0.3s ease;
        }

        .card:hover {
            transform: translateY(-5px);
        }

        .card h2 {
            font-size: 18px;
            color: #2d3748;
            margin-bottom: 20px;
            padding-bottom: 15px;
            border-bottom: 1px solid #e2e8f0;
        }

        .balance-card {
            background: linear-gradient(135deg, #38a169 0%, #48bb78 100%);
            color: white;
        }

        .balance-card h2 {
            color: white;
            border-bottom: 1px solid rgba(255, 255, 255, 0.2);
        }

        .balance-amount {
            font-size: 42px;
            font-weight: 700;
            margin: 20px 0;
            text-align: center;
        }

        .stats-container {
            display: grid;
            grid-template-columns: 1fr 1fr;
            gap: 15px;
        }

        .stat-item {
            text-align: center;
            padding: 15px;
            background-color: #f7fafc;
            border-radius: 12px;
        }

        .stat-value {
            font-size: 24px;
            font-weight: 700;
            color: #38a169;
            margin-bottom: 5px;
        }

        .stat-label {
            font-size: 14px;
            color: #718096;
        }

        .update-info {
            text-align: center;
            margin-top: 20px;
            font-size: 14px;
            color: #a0aec0;
        }

        .referral-card {
            position: relative;
            overflow: hidden;
        }

        .referral-code {
            background-color: #f0fff4;
            border: 2px dashed #38a169;
            border-radius: 12px;
            padding: 15px;
            text-align: center;
            margin: 20px 0;
            font-size: 24px;
            font-weight: 700;
            color: #2d3748;
            letter-spacing: 2px;
        }

        .referral-cta {
            text-align: center;
            background-color: #38a169;
            color: white;
            padding: 12px;
            border-radius: 10px;
            font-weight: 600;
            margin-top: 20px;
            cursor: pointer;
            transition: background-color 0.3s ease;
        }

        .referral-cta:hover {
            background-color: #2f855a;
        }

        .transaction-history {
            margin-top: 40px;
        }

        .transaction-history h2 {
            font-size: 24px;
            color: #2d3748;
            margin-bottom: 20px;
            padding-bottom: 15px;
            border-bottom: 1px solid #e2e8f0;
        }

        .transaction-list {
            list-style: none;
        }

        .transaction-item {
            display: flex;
            justify-content: space-between;
            align-items: center;
            padding: 20px;
            border-bottom: 1px solid #e2e8f0;
            transition: background-color 0.3s ease;
        }

        .transaction-item:hover {
            background-color: #f7fafc;
        }

        .transaction-info {
            display: flex;
            align-items: center;
        }

        .transaction-icon {
            width: 50px;
            height: 50px;
            border-radius: 12px;
            display: flex;
            align-items: center;
            justify-content: center;
            margin-right: 15px;
            background-color: #f0fff4;
            color: #38a169;
            font-size: 20px;
        }

        .transaction-details h3 {
            font-size: 16px;
            color: #2d3748;
            margin-bottom: 5px;
        }

        .transaction-details p {
            font-size: 14px;
            color: #718096;
        }

        .transaction-amount {
            font-weight: 700;
            font-size: 18px;
        }

        .credit {
            color: #38a169;
        }

        .debit {
            color: #e53e3e;
        }

        .empty-state {
            text-align: center;
            padding: 40px 0;
            color: #a0aec0;
        }

        .empty-state i {
            font-size: 64px;
            margin-bottom: 20px;
            opacity: 0.5;
        }

        .empty-state p {
            font-size: 18px;
        }

        @media (max-width: 768px) {
            .wallet-grid {
                grid-template-columns: 1fr;
            }

            .stats-container {
                grid-template-columns: 1fr;
            }

            .balance-amount {
                font-size: 32px;
            }
        }
    </style>

</head>

<body>
    <%- include('../../views/partials/user/header',{title:'Wishlist'}) %>
        <div class="container">
            <header>
                <div class="logo">Green<span>Wallet</span></div>
                <div class="user-info">
                    <img class="user-avatar"
                        src="<%= user.userProfileImage || 'https://ui-avatars.com/api/?name=' + encodeURIComponent(user.username) + '&background=2e7d32&color=fff&size=160' %>">
                    <div>
                        <%= user.username %>
                    </div>
                </div>
            </header>

            <div class="wallet-header">
                <h1>YOUR WALLET</h1>
                <p>Manage your wallet and transactions</p>
            </div>

            <div class="wallet-grid">
                <div class="card balance-card">
                    <h2>Wallet Balance</h2>
                    <div class="balance-amount">₹ <%= wallet.balance     %>
                    </div>

                    <div class="stats-container">
                        <div class="stat-item">
                            <div class="stat-value">₹ <%= wallet.totalCredited %>
                            </div>
                            <div class="stat-label">TOTAL CREDITED</div>
                        </div>
                        <div class="stat-item">
                            <div class="stat-value">₹ <%= wallet.totalDebited %>
                            </div>
                            <div class="stat-label">TOTAL DEBITED</div>
                        </div>
                    </div>


                </div>

                <div class="card referral-card">
                    <h2>Your Referral Code</h2>
                    <p>Share your code with friends and earn rewards</p>

                    <div class="referral-code">
                        <%= user.referralCode %>
                    </div>

                    <div class="referral-cta">
                        EARN ₹100 FOR EACH SUCCESSFUL REFERRAL
                    </div>
                </div>
            </div>

            <div class="transaction-history">
                <% if(wallet.transaction?.length<1){ %>
                    <h2>Transaction History</h2>

                    <div class="empty-state">
                        <i class="fas fa-receipt"></i>
                        <p>No transactions yet</p>
                    </div>

                    <% }else{ %>


                        <% wallet.transactions?.forEach((transaction)=>{ %>
                            <% if(transaction.type==='credit' ){%>
                                <li class="transaction-item">
                                    <div class="transaction-info">
                                        <div class="transaction-icon">
                                            <i class="bi bi-credit-card"></i>
                                        </div>
                                        <div class="transaction-details">
                                            <h3>Deposit</h3>
                                            <p>
                                                <%= new Date(transaction.createdAt).toLocaleString('en-IN', {
                                                    weekday: 'short' , year: 'numeric' , month: 'short' , day: 'numeric'
                                                    , hour: '2-digit' , minute: '2-digit' }) %> • <%= transaction.reason %>
                                            </p>

                                        </div>
                                    </div>
                                    <div class="transaction-amount credit">+₹ <%= transaction.amount %>
                                    </div>
                                </li>
                                <% }else if(transaction.type==='debit'){ %>
                                    <li class="transaction-item">
                                            <div class="transaction-info">
                                                <div class="transaction-icon">
                                                    <i class="bi bi-credit-card-2-front"></i>

                                                </div>
                                                <div class="transaction-details">
                                                    <h3>Purchase</h3>
                                                    <p>
                                                <%= new Date(transaction.createdAt).toLocaleString('en-IN', {
                                                    weekday: 'short' , year: 'numeric' , month: 'short' , day: 'numeric'
                                                    , hour: '2-digit' , minute: '2-digit' }) %> • <%= transaction.reason %>
                                            </p>
                                                   
                                                </div>
                                            </div>
                                            <div class="transaction-amount debit">-₹ <%= transaction.amount %></div>
                                        </li>
                                    <% } %>


                                    <!-- <ul class="transaction-list" style="display: block;">


                                        

                                        <li class="transaction-item">
                                            <div class="transaction-info">
                                                <div class="transaction-icon">
                                                    <i class="fas fa-gift"></i>
                                                </div>
                                                <div class="transaction-details">
                                                    <h3>Referral Bonus</h3>
                                                    <p>Sep 5, 2023 • Referral reward</p>
                                                </div>
                                            </div>
                                            <div class="transaction-amount credit">+₹ 100</div>
                                        </li>
                                    </ul> -->
                                    <% }) %>
                                        <% } %>
            </div>
        </div>

        <script>
            // Simple animation for elements
            document.addEventListener('DOMContentLoaded', function () {
                const cards = document.querySelectorAll('.card')
                cards.forEach((card, index) => {
                    card.style.animation = `fadeIn 0.5s ease ${index * 0.2}s forwards`
                    card.style.opacity = '0'
                });

                // Add animation keyframes
                const style = document.createElement('style')
                style.textContent = `
                @keyframes fadeIn {
                    from { opacity: 0; transform: translateY(20px); }
                    to { opacity: 1; transform: translateY(0); }
                }
            `
                document.head.appendChild(style)
            })
        </script>
</body>

</html>