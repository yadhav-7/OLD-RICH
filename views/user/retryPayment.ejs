<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Retry Payment</title>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
      <link rel="icon" href="/userTitleLogo/Gemini_Generated_Image_kb9bqfkb9bqfkb9b (1).png" type="image/png">

  <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
        }

        body {
            background: linear-gradient(135deg, #f8f8f8 0%, #f0f0f0 100%);
            color: #333;
            line-height: 1.6;
            padding: 20px;
            display: flex;
            justify-content: center;
            align-items: center;
            min-height: 100vh;
        }

        .container {
            max-width: 500px;
            width: 100%;
            background-color: white;
            border-radius: 12px;
            box-shadow: 0 6px 20px rgba(0, 0, 0, 0.1);
            overflow: hidden;
        }

        .header {
            background-color: #d32f2f;
            color: white;
            padding: 25px;
            text-align: center;
            position: relative;
        }

        .header h1 {
            font-size: 26px;
            margin-bottom: 8px;
            font-weight: 600;
        }

        .header p {
            opacity: 0.9;
            font-size: 16px;
        }

        .header-icon {
            background-color: rgba(255, 255, 255, 0.2);
            width: 70px;
            height: 70px;
            border-radius: 50%;
            display: flex;
            align-items: center;
            justify-content: center;
            margin: 0 auto 15px;
            font-size: 30px;
        }

        .content {
            padding: 30px;
        }

        .message {
            background-color: #ffebee;
            border-left: 4px solid #d32f2f;
            padding: 18px;
            margin-bottom: 25px;
            border-radius: 6px;
        }

        .message h2 {
            color: #d32f2f;
            font-size: 18px;
            margin-bottom: 8px;
            display: flex;
            align-items: center;
            gap: 8px;
        }

        .payment-details {
            background-color: #f9f9f9;
            padding: 20px;
            border-radius: 8px;
            margin-bottom: 25px;
            border: 1px solid #eee;
        }

        .payment-details h3 {
            margin-bottom: 15px;
            color: #444;
            font-size: 18px;
            display: flex;
            align-items: center;
            gap: 10px;
        }

        .detail-row {
            display: flex;
            justify-content: space-between;
            padding: 12px 0;
            border-bottom: 1px solid #eee;
        }

        .detail-row:last-child {
            border-bottom: none;
            font-weight: 600;
            color: #d32f2f;
            font-size: 18px;
        }

        .instructions {
            margin-bottom: 30px;
        }

        .instructions h3 {
            margin-bottom: 15px;
            color: #444;
            font-size: 18px;
            display: flex;
            align-items: center;
            gap: 10px;
        }

        .instructions ol {
            padding-left: 22px;
        }

        .instructions li {
            margin-bottom: 10px;
            padding-left: 8px;
        }

        .payment-methods {
            display: flex;
            gap: 15px;
            margin-bottom: 25px;
            justify-content: center;
        }

        .payment-method {
            border: 1px solid #ddd;
            border-radius: 8px;
            padding: 12px;
            width: 80px;
            text-align: center;
            transition: all 0.3s;
            cursor: pointer;
        }

        .payment-method:hover {
            border-color: #d32f2f;
            transform: translateY(-3px);
        }

        .payment-method.active {
            border-color: #d32f2f;
            background-color: #ffebee;
        }

        .payment-method i {
            font-size: 28px;
            margin-bottom: 8px;
            color: #555;
        }

        .payment-method span {
            font-size: 12px;
            display: block;
        }

        .retry-button {
            display: block;
            width: 100%;
            padding: 16px;
            background-color: #d32f2f;
            color: white;
            border: none;
            border-radius: 8px;
            font-size: 17px;
            font-weight: 600;
            cursor: pointer;
            transition: background-color 0.3s;
            text-align: center;
            text-decoration: none;
            margin-bottom: 20px;
            box-shadow: 0 4px 6px rgba(211, 47, 47, 0.2);
        }

        .retry-button:hover {
            background-color: #b71c1c;
            transform: translateY(-2px);
            box-shadow: 0 6px 8px rgba(211, 47, 47, 0.3);
        }

        .support {
            text-align: center;
            font-size: 15px;
            color: #666;
            margin-top: 10px;
        }

        .home-btn {
            display: block;
            width: 100%;
            padding: 16px;
            background-color: #28752a;
            color: white;
            border: none;
            border-radius: 8px;
            font-size: 17px;
            font-weight: 600;
            cursor: pointer;
            transition: background-color 0.3s;
            text-align: center;
            text-decoration: none;
            margin-bottom: 20px;
            box-shadow: 0 4px 6px rgba(211, 47, 47, 0.2);
        }

        .home-btn:hover {
            background-color: #0b3919;
            transform: translateY(-2px);
            box-shadow: 0 6px 8px rgba(28, 118, 63, 0.3);
        }
        .support a {
            color: #d32f2f;
            text-decoration: none;
            font-weight: 500;
        }

        .secure-notice {
            display: flex;
            align-items: center;
            justify-content: center;
            margin-top: 15px;
            font-size: 14px;
            color: #666;
            gap: 8px;
        }

        .secure-notice i {
            color: #d32f2f;
        }

        @media (max-width: 480px) {
            body {
                padding: 10px;
            }

            .container {
                border-radius: 10px;
            }

            .content {
                padding: 20px;
            }

            .payment-methods {
                flex-wrap: wrap;
            }
        }
    </style>
</head>

<body>
    <div class="container">
        <div class="header">
            <div class="header-icon">
                <i class="fas fa-credit-card"></i>
            </div>
            <h1>Retry Payment</h1>
            <p>Complete your transaction securely</p>
        </div>

        <div class="content">
            <div class="message">
                <h2><i class="fas fa-exclamation-circle"></i> Payment Failed</h2>
                <p>We encountered an issue processing your payment. Please try again with the same or a different
                    payment method.</p>
            </div>

            <div class="payment-details">
                <h3><i class="fas fa-receipt"></i> Transaction Details</h3>
                <div class="detail-row">
                    <span>Order ID:</span>
                    <span>
                        <%= order.orderId %>
                    </span>
                </div>
                <div class="detail-row">
                    <span>Date:</span>
                    <span>
                        <%= new Date(order.createdOn).toLocaleString() %>
                    </span>
                </div>

                <div class="detail-row">
                    <span>Payment Method:</span>
                    <span>
                        <% if (order.paymentMethod==='RPAY' ) { %>
                            Razorpay
                            <% } else { %>
                                Online
                                <% } %>
                    </span>

                </div>
                <div class="detail-row">
                    <span>Amount:</span>
                    <span>₹<%= order.finalAmount %></span>
                </div>
            </div>

            <a href="/paymentFaildRetry?orderId=<%= order.orderId %>" class="retry-button">Retry Payment Securely</a>

            <a href="/home" class="home-btn">home</a>
            <div class="secure-notice">
                <i class="fas fa-lock"></i>
                <span>Your payment information is secure and encrypted</span>
            </div>

            <div class="support">
                <p>Need help? <a href="#">Contact our support team</a></p>
            </div>
        </div>
    </div>

    <script>
        // Payment method selection
        const paymentMethods = document.querySelectorAll('.payment-method');
        paymentMethods.forEach(method => {
            method.addEventListener('click', () => {
                paymentMethods.forEach(m => m.classList.remove('active'));
                method.classList.add('active');
            });
        });

        // Retry payment button functionality
        // document.querySelector('.retry-button').addEventListener('click', function (e) {
        //     e.preventDefault();

        //     // Show loading state
        //     const button = this;
        //     const originalText = button.textContent;
        //     button.textContent = 'Processing...';
        //     button.style.opacity = '0.8';

        //     // Simulate API call
        //     setTimeout(() => {
        //         // For demo purposes, we'll simulate a successful payment
        //         button.textContent = 'Payment Successful!';
        //         button.style.backgroundColor = '#d32f2f'; // Keep red for consistency

        //         // Show success message
        //         const messageDiv = document.querySelector('.message');
        //         messageDiv.innerHTML = `
        //             <h2><i class="fas fa-check-circle"></i> Payment Successful</h2>
        //             <p>Your payment has been processed successfully. Thank you for your purchase!</p>
        //         `;
        //         messageDiv.style.backgroundColor = '#ffebee';
        //         messageDiv.style.borderLeftColor = '#d32f2f';

        //         // Redirect after success (commented out for demo)
        //         // setTimeout(() => {
        //         //     window.location.href = '/success';
        //         // }, 2000);
        //     }, 2000);
        // });
  
  </script>
</body>

</html>