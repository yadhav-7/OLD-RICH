<!DOCTYPE html>
<html>
<head>
<style>
:root {
  --sidebar-bg: #2c3e50;
  --text-color: #ecf0f1;
  --hover-bg: #34495e;
  --active-bg: #1abc9c;
  --transition: all 0.3s ease;
  --sidebar-width: 250px;
  --collapsed-width: 70px;
}

.sidebar {
  background-color: var(--sidebar-bg);
  width: var(--sidebar-width);
  height: 100vh;
  position: fixed;
  top: 0;
  left: 0;
  overflow-y: auto;
  padding-top: 20px;
  box-shadow: 2px 0 5px rgba(0, 0, 0, 0.2);
  transition: var(--transition);
  z-index: 100;
}

.sidebar-header {
  padding: 15px 20px;
  color: white;
  font-size: 1.2rem;
  font-weight: bold;
  text-align: center;
  border-bottom: 1px solid rgba(255,255,255,0.1);
  margin-bottom: 15px;
}

.sidebar-nav {
  list-style: none;
  padding: 0;
  margin: 0;
}

.sidebar-nav li {
  margin: 8px 0;
}

.sidebar-nav a {
  display: flex;
  align-items: center;
  padding: 12px 20px;
  color: var(--text-color);
  text-decoration: none;
  font-size: 15px;
  transition: var(--transition);
  border-left: 3px solid transparent;
}

.sidebar-nav a i {
  margin-right: 15px;
  font-size: 18px;
  min-width: 20px;
  text-align: center;
}

.sidebar-nav a:hover {
  background-color: var(--hover-bg);
  padding-left: 25px;
}

.sidebar-nav a.active {
  background-color: var(--active-bg);
  border-left-color: white;
}

/* Collapsed State */
.sidebar.collapsed {
  width: var(--collapsed-width);
}

.sidebar.collapsed .sidebar-header,
.sidebar.collapsed .sidebar-nav a span {
  display: none;
}

.sidebar.collapsed .sidebar-nav a {
  justify-content: center;
  padding: 15px 0;
}

.sidebar.collapsed .sidebar-nav a i {
  margin-right: 0;
}

/* Responsive */
@media (max-width: 768px) {
  .sidebar {
    transform: translateX(-100%);
  }
  
  .sidebar.open {
    transform: translateX(0);
  }
}
</style>
</head>
<body>
<aside class="sidebar">
  <div class="sidebar-header">OLD RICH</div>
  <ul class="sidebar-nav">
    <li><a href="/admin/dashboard"><i class="fas fa-tachometer-alt"></i> <span>Dashboard</span></a></li>
    <li><a href="/admin/users"><i class="fas fa-users"></i> <span>Customers</span></a></li>
    <li><a href="/admin/category"><i class="fas fa-folder-open"></i> <span>Category</span></a></li>
    <li><a href="/admin/getAddProduct"><i class="fas fa-plus-circle"></i> <span>Add Product</span></a></li>
    <li><a href="/admin/getAllProducts"><i class="fas fa-boxes"></i> <span>Products</span></a></li>
    <li><a href="/admin/coupons"><i class="fas fa-ticket-alt"></i> <span>Coupon</span></a></li>
    <li><a href="/admin/banners"><i class="fas fa-image"></i> <span>Banner</span></a></li>
    <li><a href="/admin/orders"><i class="fas fa-shopping-cart"></i> <span>Orders</span></a></li>
  </ul>
</aside>

<!-- Font Awesome for icons -->
<script src="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.2/js/all.min.js"></script>
</body>
</html>